import { requireNativeViewManager } from 'expo-modules-core';
import * as React from 'react';
import { Platform, View } from 'react-native';
let NativeView;
if (Platform.OS === 'ios') {
    NativeView = requireNativeViewManager('ExpoAvRoutePickerView');
}
else {
    NativeView = View;
}
export default function ExpoAvRoutePickerView(props) {
    return (<View {...props}>
      <NativeView {...props}/>
    </View>);
}
//# sourceMappingURL=ExpoAvRoutePickerView.js.map